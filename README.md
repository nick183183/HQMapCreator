## Установка

```bash
python -m venv .venv
.venv\Scripts\activate  # Windows
pip install -r requirements.txt
```

## Использование
```bash
python appTest.py
```

## Горячие клавиши:
```bash
F9 — выбрать область
F6 — захват и склейка
F4 — откат
F7 — выход
F8 — сохранить
```

